import Section from "../components/Section";


/** Страница при переходе на некорректный путь */
const NotFoundPage = () => {
    return (
        <Section title="404 Простите, но мы ничего не нашли :(">
            
        </Section>
    );
}

export default NotFoundPage;